// Main Color
const HUE = 64 // Math.round(100 * Math.random())  // I love yellow whose hue is 12.
const SAT = 100 // Math.round(100 * Math.random()) // 100
const BRI = 100 //Math.round(100 * Math.random()) // 100

// Gray Scales
const WHITE = "#FFF"
const LIGHT_GRAY = "#BBB"
const GRAY = "#777"
const DARK_GRAY = "#333"
const BLACK = "#000"

// Transparent
const TRANSP = "#0000"

// Iconic Colors
const WINDOWS_95_DESKTOP_COLOR = "#00807F"

const WINDOWS_95_LOGO_RED = "#FF3300"
const WINDOWS_95_LOGO_BLUE = "#02A5FF"
const WINDOWS_95_LOGO_YELLOW = "#FFCC00"
const WINDOWS_95_LOGO_GREEN = "#66CC33"
const WINDOWS_95_LOGO_BLACK = "#000000"
