// Main Color
const HUE = 12 // Math.round(100 * Math.random())  // I love yellow whose hue is 12.
const SAT = 100 // Math.round(100 * Math.random()) // 100
const BRI = 100 // Math.round(100 * Math.random()) // 100

// Gray Scales
const WHITE = "#FFF"
const LIGHT_GRAY = "#BBB"
const GRAY = "#777"
const DARK_GRAY = "#333"
const BLACK = "#000"

// Transparent
const TRANSP = "#0000"